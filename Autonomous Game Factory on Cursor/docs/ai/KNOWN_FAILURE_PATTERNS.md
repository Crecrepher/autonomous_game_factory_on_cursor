# Known Failure Patterns

- ModuleStructureValidator initially failed due to YAML regex mismatch
- Test detection needed recursive search under Tests/
- Registry/path mismatch caused false warnings before cleanup
